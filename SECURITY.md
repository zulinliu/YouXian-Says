# 安全政策

## 支持的版本

| 版本 | 支持状态 |
|------|---------|
| v1.x | :white_check_mark: 当前支持 |

## 报告安全漏洞

我们非常重视安全问题。如果您发现安全漏洞，**请不要通过公开的 GitHub Issues 报告**。

### 报告流程

1. **通过 GitHub Security Advisories 报告**（推荐）：
   - 前往 [Security Advisories](../../security/advisories/new)
   - 填写漏洞详情、影响范围和复现步骤

2. **通过 Email 报告**：
   - 发送邮件至项目维护者（通过 [GitHub Profile](https://github.com/zulinliu) 获取联系方式）
   - 邮件标题请注明 `[Security]`
   - 包含漏洞描述、影响范围、复现步骤（如适用）

### 报告内容

请尽可能包含以下信息：

- 漏洞类型（如 SQL 注入、XSS、信息泄露等）
- 漏洞影响的组件或文件路径
- 复现步骤
- 潜在影响
- 如有可能，提供修复建议

### 响应时间

- **确认收悉**：48 小时内
- **初步评估**：7 天内
- **修复计划**：根据严重程度，14 天内提供

### 处理原则

- 所有安全报告将被严格保密
- 我们会在修复并发布补丁后公开致谢报告者（经同意）
- 我们不会对善意报告安全漏洞的人采取法律行动

## 安全最佳实践

使用本项目时，请注意：

- **API Key 管理**：所有 API Key 必须通过 `.env` 文件配置，禁止硬编码到代码中
- **数据库安全**：生产环境请使用加密连接，定期备份
- **依赖更新**：定期运行 `pip audit` 检查依赖中的已知漏洞
- **访问控制**：修改 `.env` 中的默认密码 `ADMIN_PASSWORD`

## 已知安全措施

本项目已实施以下安全措施：

- JWT 认证机制（FastAPI 接口）
- 密码认证（Streamlit 管理后台）
- SQL 参数化查询（防止 SQL 注入）
- 模型参数加密存储（`cryptography.fernet`）
- `.gitignore` 排除敏感文件（`.env`、数据库、模型状态）
